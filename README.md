# Proto-Vorlage AI App

## Install & Run
```bash
pip install streamlit
streamlit run app.py
```

## Project Structure
- `agents/` – textual agents
- `data/` – sample verses
- `reconstruction.py` – alignment logic
