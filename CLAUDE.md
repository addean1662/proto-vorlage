# proto-vorlage

Next.js 15 app with React 19, Tailwind CSS, TypeScript, and Anthropic SDK.

## Dev Server

```
npm run dev
```

Runs on `http://localhost:3000` (or next available port if 3000 is taken).

## Node.js

Node v22 is at `C:\Users\addea\AppData\Local\nodejs\node-v22.14.0-win-x64\`.
The `.claude/settings.json` injects this into PATH automatically each session.
If `npm` is ever not found, verify that path still exists.
